# Next.js App Router on Netlify
# This rule ensures that all requests not matching a static file
# are handled by Next.js's rendering logic.
# It's essential for dynamic routes and client-side navigation.
/* /index.html 200

# Optional: If you have specific redirects, add them above this rule.
# Example:
# /old-path /new-path 301
